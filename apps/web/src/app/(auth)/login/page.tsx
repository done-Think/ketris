import { PagePlaceholder } from '@shared/components/ui'

export const metadata = { title: 'Ketris' }

export default function LoginPage() {
  return (
    <PagePlaceholder
      title="Entrar"
      description="Formulário de login (React Hook Form + Zod + NextAuth) — a implementar."
      maxWidth="sm"
      paddingY={8}
    />
  )
}

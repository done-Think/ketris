export type PublicPropertiesPageProps = {
  searchParams?: {
    finalidade?: string
    localizacao?: string
  }
}

export type BrokerPageProps = {
  params: {
    id: string
  }
}

export type AgencyPageProps = {
  params: {
    id: string
  }
}
